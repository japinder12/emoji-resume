export default function Legend() {
  return (
    <p className="text-xs text-slate-500">
      Tip: Upload a PDF or paste text. Reorder extracted lines if needed, then export or share a permalink.
    </p>
  );
}
