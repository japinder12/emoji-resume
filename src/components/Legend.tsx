export default function Legend() {
  return (
    <p className="text-xs text-slate-500">
      Upload a PDF or paste text. Reorder extracted lines if needed, then export or copy a permalink.
    </p>
  );
}
